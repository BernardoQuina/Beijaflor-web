import { NextPage } from 'next'
import { Layout } from '../../../components/Layout'

interface mainProps {

}

const main: NextPage<mainProps> = ({  }) => {
  return (
    <Layout>
      hello main
    </Layout>
  )
}

export default main
