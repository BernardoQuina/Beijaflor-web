mutation ToggleFromWishList(
  $wishListId: String!
  $productId: String!
  $merge: Boolean
) {
  toggleFromWishList(
    wishListId: $wishListId
    productId: $productId
    merge: $merge
  ) {
    ...BasicWishListInfo
  }
}
